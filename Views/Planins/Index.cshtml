@model IEnumerable<Ikigenda.Models.Planin>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Notas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Disponible)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaLocal)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Notas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Disponible)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaLocal)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PlaninId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PlaninId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PlaninId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
